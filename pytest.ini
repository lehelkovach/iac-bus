[pytest]
testpaths = tests
markers =
    swarm: multi-agent swarm behaviour; starts a real HTTP bus and runs concurrent agents
log_cli = false
filterwarnings =
    ignore::DeprecationWarning:jsonschema.*
